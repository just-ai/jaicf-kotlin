{
  "object": "page",
  "entry": [
    {
      "id": "{{ randomInt }}",
      "time": "{{ timestamp }}",
      "messaging": [
        {
          "sender": {
            "id": "{{ clientId }}"
          },
          "recipient": {
            "id": "{{ clientId }}"
          },
          "timestamp": "{{ timestamp }}",
          "message": {
            "mid": "{{ messageId }}",
            "text": "{{ text }}"
          }
        }
      ]
    }
  ]
}